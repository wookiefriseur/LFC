WIP - DO NOT USE

# LibFurnitureCatalogue

Furniture Catalogue database library. Used by FurnitureCatalogue.

This repo will contain the following:

1. AddOn src files in (./LibFurnitureCatalogue)[./LibFurnitureCatalogue]
2. human-readable DB files
3. helper scripts to build compact DB files from human readable ones
4. webinterface for contributors and lookups
5. ???

## Release

- bundled src files from `./LibFurnitureCatalogue`


## Tests

- `tests/run_static.sh`: syntax
- `validate_data.lua`: duplication + schema

